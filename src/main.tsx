import React from "react";
import ReactDOM from "react-dom/client";
import WarehouseCalculator from "../warehouse_calculator";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WarehouseCalculator />
  </React.StrictMode>
);
